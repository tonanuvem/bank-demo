# Versoes fixas de proposito. Sem isto, um rebuild em outra data instala outra
# coisa -- e o modo de falha e' silencioso: a aplicacao sobe, os paineis ficam
# vazios e ninguem tocou em nada. E' a versao da distro OTel que decide a
# convencao semantica dos atributos (http.status_code x
# http.response.status_code), e toda query do laboratorio depende dela.
#
# Para atualizar: suba o ambiente, confirme que os paineis respondem, rode
# `pip freeze` / `npm ls --depth=0` no container e traga os numeros para ca.

Flask==3.1.3
Flask-Cors==6.0.5
grpcio==1.84.0
grpcio-tools==1.84.0
pymongo==4.18.1
pytest==9.1.1
requests==2.34.2
dotmap==1.3.30
python-dotenv==1.2.3
